import SwiftUI

import Authorization
import Networking

struct ContentView: View {
    var body: some View {
        NavigationStack {
            MainView()
        }
    }
}

#Preview {
    ContentView()
}
